<h1>Login</h1>

<%= form_for @user, url: sessions_create_path do |f| %>

	<%= f.label :name %>
	<%= f.text_field :name %>

	<%= f.label :password %>
	<%= f.password_field :password %>

	<%= f.submit "Sign In" %>

<% end %>
